---
  - name: "configure Jenkins on local host"
    hosts: localhost
    connection: local
    gather_facts: false

    tasks:
    - name: "Call jenkins role"
      import_role:
        name: jenkins
#        tasks_from: admin_user_config.yml
  

